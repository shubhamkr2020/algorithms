/*

bool res = false;

for(i=0 to all possible cases) {
  mark something/multiple things and

  // do something logical like:
  res = res || (try for something using the above marked)
  
  unmark all those things
}

return res;




*/
