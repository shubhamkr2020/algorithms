// Use mask to check more than two flag

// For two flags:
mask = mask|1		// for first flag
mask = mask|2		// for second flag


// For three flags:
mask = mask|1		// for first
mask = mask|2		// for second
mask = mask|4		// for third